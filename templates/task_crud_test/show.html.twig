{% extends 'base.html.twig' %}

{% block title %}Task{% endblock %}

{% block body %}
    <h1>Tache</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ task.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ task.name }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_task_crud_test_index') }}">back to list</a>

    <a href="{{ path('app_task_crud_test_edit', {'id': task.id}) }}">Editer</a>

    {{ include('task_crud_test/_delete_form.html.twig') }}
{% endblock %}
